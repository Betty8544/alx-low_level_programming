#include "main.h"

/**
 * _isdigit - checks for a digit
 * @c: The digit to be computed
 *
 * Return: 1 if c is a digit
 */
int _isdigit(int c)
{
	for (c = 0; c <= 9; c++)
	{
		return (1);
	}

	return (0);
}
