#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: The character to be checked
 *
 * Return: 1 if c is uppercase.
 */
int _isupper(int c)
{
	for (c = 65; c <= 90; c++)
	{
		return (1);
	}

	return (0);
}
